#include "main.h"
/**
 * add - adds two integers
 * Return: a result
 * @l: is a first variable
 * @z: is a second variable
 */
int add(int l, int z)
{
int a;
int j;
int n;
int k;
int i;
int v;
if (l == -)
{
a = l * -1;
j = z + a;
_putchar(j);
}
else if (z == -)
{
n = z * -1;
k = n + l;
_putchar(k);
}
else if ((l == -) &&(z == -))
{
i = a + n;
_putchar(i);
}
else
{
v = l + z;
_putchar(v);
}
}
_putchar('\n');
return (j);
return (k);
return (i);
return (v);
}
