#include "main.h"
/**
 *_abs - computes the absolute value of an integer
 *Return: Always 0
 *@n: is a variable
 */
int _abs(int n)
{
int v = n * -;
if (n < 0)
{
_putchar(v);
}
else
{
_putchar(n);
}
return (0);
}
