#include "main.h"
/**
 * div - divides a two integer.
 * @a: the first number.
 * @b: the second number.
 * Return: the number.
 */
int div(int a, int b)
{
	int divi;

	divi = a / b;
	return (divi);
}
