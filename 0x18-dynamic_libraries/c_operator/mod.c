#include "main.h"
/**
 * mod - finds remainder outside the division of two numbers.
 * @a: the first number.
 * @b: the second number.
 * Return: the number
 */
int mod(int a, int b)
{
	int mod;

	mod = a % b;
	return (mod);
}
