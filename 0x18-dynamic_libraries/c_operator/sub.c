#include "main.h"
/**
 * sub - substracts a two integer.
 * @a: the first number.
 * @b: the second number.
 * Return: the number
 */
int sub(int a, int b)
{
	int sub;

	sub = a - b;
	return (sub);
}
